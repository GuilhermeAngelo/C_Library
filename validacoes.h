//Recebe uma string composta apenas por números e valida se é um cpf. Se for retorna 1, caso contrário retorna 0.
int validaCPF(char[]);
//Recebe uma string composta letras sem caracteres especiais e valida se é um nome. Se for retorna 1, caso contrário retorna 0.
int validaNome(char[]);
//Recebe uma string composta letras sem caracteres especiais e valida se é um email. Se for retorna 1, caso contrário retorna 0.
int validaEmail(char[]);
//Recebe uma string composta letras sem caracteres especiais e núemeros ou apenas letras e valida se é um nome. Se for retorna 1, caso contrário retorna 0.
int validaUsuario(char[]);
//Recebe uma string composta letras sem caracteres especiais e núemeros ou apenas letras e valida se é uma senha válida. Se for retorna 1, caso contrário retorna 0.
int validaSenha(char[]);
//Recebe uma string composta letras sem caracteres especiais e valida se é um nome. Se for retorna 1, caso contrário retorna 0.
int validaAutor(char[]);

int converteDia(char[]);

int converteMes(char[]);

int converteAno(char[]);

int bissexto(int);

int testaData(int, int, int);

int validaEdicao(char[]);

int validaIsbn(char[]);

int validaTitulo(char[]);